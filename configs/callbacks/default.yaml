model_checkpoint:
  # _target_: pytorch_lightning.callbacks.ModelCheckpoint
  # monitor: "train/g_loss"
  # dirpath: "ckpts"
  # filename: "best-{epoch:02d}-{train/g_loss:.4f}"
  # every_n_train_steps: 10000
  # save_top_k: 1
  # mode: "min"
  # save_last: True
  # save_on_train_epoch_end: True
  # auto_insert_metric_name: False
  _target_: pytorch_lightning.callbacks.ModelCheckpoint
  monitor: "train/d_loss"
  dirpath: "/home/users/queenie/workspace/ai-take-home/checkpoints/"
  filename: "best-{epoch:02d}-{train/g_loss:.4f}"
  every_n_train_steps: 10000
  save_top_k: -1
  save_last: True
  mode: "min"